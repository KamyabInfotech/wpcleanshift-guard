# CleanShift Guard (packaging repo)

> **Monorepo source of truth:** Guard runtime and control-plane tip live in `KamyabInfotech/wpcleanshift`. This repo's VERSION and Guard PHP `Version` / `CLEANSHIFT_GUARD_VERSION` track the monorepo tip for packaging alignment (synced at `1.0.0-rc.417`). Full tree sync remains deferred.

R1 commercial self-serve still requires branded Stage −1 restore on the monorepo control plane.
